import { createStore } from "vuex";
import { NotifyModalStore } from "@/store/notify-modal-store";
export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: { NotifyModalStore },
});
